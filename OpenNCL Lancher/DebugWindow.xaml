<Window
    x:Class="OpenNCL_Lancher.DebugWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenNCL_Lancher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="OpenNCL Backend Debug">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Status:" VerticalAlignment="Center" Opacity="0.7" />
            <TextBlock x:Name="StatusLine" Text="-" VerticalAlignment="Center" />
            <Button x:Name="ClearBtn" Content="Clear" Click="ClearBtn_Click" />
            <Button x:Name="CopyBtn" Content="Copy All" Click="CopyBtn_Click" />
        </StackPanel>

        <ListView Grid.Row="1" x:Name="EventsList" FontFamily="Cascadia Code, Consolas, monospace" FontSize="12">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:DebugRow">
                    <TextBlock Text="{x:Bind Line}" TextWrapping="Wrap" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
